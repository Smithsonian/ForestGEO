---
title: Understanding Species
description: How the Species List works, how to add and remove species, and what changed in 2026.
---

The **Species List** is the per-site catalog of every species that can be referenced from a measurement. Every measurement row's `SpeciesCode` must resolve to an entry in this list — if it doesn't, the row will fail validation as **SpCode invalid** until you either add the species or correct the code.

The Species List lives under **Stem & Plot Details → Species List** in the sidebar.

## What a species record contains

| Field | Notes |
|---|---|
| **Species Code** | The short code your CSVs reference (e.g. `CRUD02`). Up to 25 characters. Not case-sensitive. |
| **Genus & Species** | Full Latin binomial. |
| **Family** | Taxonomic family. Stored separately so multiple genera can share a family. |
| **Authority** | Naming authority for the species. |
| **IDLevel** | How precise the identification is (species, genus, family, morpho, etc.). |
| **Valid Code** | A free-text note recording the accepted name where this entry is a synonym. |

## Adding species

Open **Stem & Plot Details → Species List**. Both routes start from that grid's toolbar:

1. **One at a time.** Click **Manual Entry Form** and fill in at minimum a Species Code, Genus, and Species.
2. **Bulk upload.** Click **Upload** on the same toolbar and supply a CSV. The upload rejects files containing duplicate species codes before anything is inserted.

Each row also carries a **Limits** action, which opens the species limits dialog for setting
plausible minimum and maximum DBH values used during validation.

## Changes shipped in 2026

The Species List had several long-standing rough edges that were fixed in spring 2026:

- **Duplicate detection on upload.** Species uploads now **reject** files that contain duplicate `SpeciesCode` rows before any insert happens. Earlier, duplicates could be silently merged or one would overwrite the other.
- **Duplicate rows can be deleted.** If your site already had duplicate species rows (a common legacy issue), you can now select and delete the redundant rows directly from the grid. Earlier, the delete path didn't handle the duplicates safely.
- **Lock-wait retries.** Concurrent species uploads used to occasionally hang with stale-transaction lock waits. The upload now retries transient lock waits transparently — you'll see a brief pause rather than a hard failure.

## Common pitfalls

- **Species codes are not case-sensitive.** `CRUD2` and `crud2` are treated as the *same* code, so you cannot use capitalisation to distinguish two species — the second one will collide with the first. If a measurement upload reports SpCode invalid, check for typos and stray whitespace rather than capitalisation.
- **Species codes must be 25 characters or fewer.** Longer codes are rejected during ingestion.
- **Don't delete species that have measurements.** The grid will refuse to delete a species that's referenced by an existing measurement. There is no "mark obsolete" control in the grid — if a species should no longer be used, leave the record in place and stop referencing it in new uploads.
- **Editing taxonomy on a species in use propagates.** Changing the Genus or Family on a referenced species affects every measurement that points at it. If you're correcting a taxonomic identification, this is what you want; if you're not sure, add a new species row and migrate measurements over.

## Related pages

- [Understanding Supporting Data](/ForestGEO/starting/understanding-supporting-data/) — the broader picture of supporting data the app needs before measurements can be uploaded.
- [Validation Errors](/ForestGEO/errors/validation-errors/) — including the attribute-code check that runs against the Stem Codes list.
- [Failed Measurements Guide](/ForestGEO/errors/failed-measurements-guide/) — how SpCode invalid rows are handled and recovered.
