/**
 * ForestGEO Documentation Custom Components
 *
 * Import components individually or use this barrel export:
 *
 * Individual imports (recommended for MDX):
 *   import ErrorReference from '../../components/ErrorReference.astro';
 *
 * Or import multiple:
 *   import ErrorReference from '../../components/ErrorReference.astro';
 *   import ProcessFlow from '../../components/ProcessFlow.astro';
 *   import ValidationRule from '../../components/ValidationRule.astro';
 *
 * Available Components:
 * ---------------------
 *
 * Phase 1 - Critical:
 *   - ErrorReference: Standardized error tables (Message/Cause/Fix)
 *   - ProcessFlow: Visual workflow diagrams with steps and branches
 *   - ValidationRule: Structured validation rule cards
 *
 * Phase 2 - High Value:
 *   - Screenshot: Annotated images with captions, borders, zoom
 *   - SideBySide: Before/After comparison layouts
 *   - Scenario: Troubleshooting case studies
 *
 * Phase 3 - Nice-to-Have:
 *   - FAQ: Accordion-style Q&A sections
 *   - ConceptCard: Standardized concept introductions
 *   - TableReference: Searchable/sortable reference tables
 *   - TroubleshootingChecklist: Interactive progress checklists
 */

// Note: Astro components cannot be re-exported from .ts files
// Import them directly in your MDX files using the paths above
export {};
