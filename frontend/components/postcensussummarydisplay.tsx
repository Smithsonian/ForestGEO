"use client";
import {useCensusContext, usePlotContext} from "@/app/contexts/userselectionprovider";

export default function PostCensusSummaryDisplay() {
  let currentPlot = usePlotContext();
  let currentCensus = useCensusContext();


}